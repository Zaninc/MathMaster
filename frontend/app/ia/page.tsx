import { RoutePlaceholder } from "@/components/shared/RoutePlaceholder";

export default function IaPage() {
  return (
    <RoutePlaceholder
      title="Math Mentor"
      description="Um mentor que não entrega apenas respostas — entende como você aprende. A apresentação conceitual chega na Etapa 6 da Sprint Frontend V1; o Math Mentor está sendo construído."
      badge="dev"
    />
  );
}
