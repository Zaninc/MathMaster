import { RoutePlaceholder } from "@/components/shared/RoutePlaceholder";

export default function GeometriaPage() {
  return (
    <RoutePlaceholder
      title="Geometria"
      description="Figuras, parâmetros e construção visual conectados ao motor de geometria analítica do backend — chega na Etapa 4 da Sprint Frontend V1."
      badge="planned"
    />
  );
}
